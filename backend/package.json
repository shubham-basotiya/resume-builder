{
  "name": "resume-generator-api",
  "version": "1.0.0",
  "description": "API for generating resumes",
  "main": "api/generate-resume.js", //  Vercel uses this, but it's more for npm
  "scripts": {
    "start": "node api/generate-resume.js", //  For local testing (optional)
    "dev": "nodemon api/generate-resume.js"   //  For local development (optional)
  },
  "dependencies": {
    "pdf-lib": "^1.17.1", //  Or your version
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}